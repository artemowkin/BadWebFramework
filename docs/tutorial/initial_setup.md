# Tutorial: Initial Set Up

After cloning this repo you need to install dependencies

```
$ pipenv sync
```

Then run the shell

```
$ pipenv shell
```

And now you can create new project / run test project
